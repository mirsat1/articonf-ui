import React from 'react'

export default function NotProvider() {
    return (
        <div className="theBody">
            <h1>You are not a dApp provider, so you are not allowed to see this route.</h1>
        </div>
    )
}